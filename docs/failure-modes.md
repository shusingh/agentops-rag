# Failure Modes

Detailed failure cases live in `failure_cases/`. Each case documents what went wrong, how the system detects it, what trace/log/eval signal appears, how to reproduce it locally, and how to mitigate it.

Current cases:

- Bad retrieval
- Unsupported answer
- Missing citation
- Tenant isolation breach
- Ingestion failure
- Model timeout
- Rate limit exceeded
